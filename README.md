
<div align="center">
<img width="1200" height="475" alt="GHBanner" src="https://github.com/user-attachments/assets/0aa67016-6eaf-458a-adb2-6e31a0763ed6" />
</div>

# FamilyBudget (Семейный Бюджет)

Современное веб-приложение для учета семейных финансов с поддержкой AI.

## 🛡️ Безопасность в публичных репозиториях

Этот проект использует Gemini API. Ключ API является секретной информацией.

### 1. Локальная разработка
Создайте файл `.env` в корне проекта. **Никогда не загружайте его в GitHub!**
```env
GEMINI_API_KEY=AIzaSy...ваш_ключ...
```
Файл `.gitignore` уже настроен так, чтобы игнорировать `.env`.

### 2. GitHub Actions (Сборка Android APK)
Для автоматической сборки APK через GitHub Actions:
1. Перейдите в **Settings** вашего репозитория.
2. Выберите **Secrets and variables** -> **Actions**.
3. Нажмите **New repository secret**.
4. Имя: `GEMINI_API_KEY`.
5. Значение: Ваш ключ `AIzaSy...`.

### 3. Ограничение ключа (Важно!)
Так как в веб-версии ключ передается клиенту, **обязательно** настройте ограничения в Google Cloud Console:
1. Зайдите в [Google Cloud Credentials](https://console.cloud.google.com/apis/credentials).
2. Выберите ваш ключ.
3. В **Application restrictions** выберите **HTTP referrers** и укажите домен вашего сайта (например, `https://my-app.vercel.app/*`).
4. Для Android выберите **Android apps** и укажите имя пакета `com.familybudget.app` и SHA-1 отпечаток сертификата.

## 🚀 Как запустить (Web)

1. **Установка зависимостей**
   ```bash
   npm install
   ```

2. **Запуск локально**
   ```bash
   npm run dev
   ```

## 📱 Сборка Android (APK)

Проект настроен для сборки через Capacitor.
```bash
# Синхронизация ассетов
npm run cap:sync

# Открытие в Android Studio
npm run cap:android
```
