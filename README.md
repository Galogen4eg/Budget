
<div align="center">
<img width="1200" height="475" alt="GHBanner" src="https://github.com/user-attachments/assets/0aa67016-6eaf-458a-adb2-6e31a0763ed6" />
</div>

# FamilyBudget (Семейный Бюджет)

Современное веб-приложение для учета семейных финансов с поддержкой AI.

## 🚨 ВАЖНО: Безопасность API Ключа

**Никогда не публикуйте файл `.env` в GitHub/GitLab!**
Google автоматически блокирует ключи, найденные в публичном коде.

**Если вы получили предупреждение "Exposed API Key":**
1. Удалите старый ключ в [Google AI Studio](https://aistudio.google.com/).
2. Создайте новый ключ.
3. Создайте в корне проекта файл `.env` и добавьте ключ туда:
   ```env
   GEMINI_API_KEY=AIzaSy...ваш_новый_ключ...
   ```
4. Убедитесь, что файл `.gitignore` содержит строчку `.env`.

## 🚀 Как запустить (Web)

1. **Установка зависимостей**
   ```bash
   npm install
   ```

2. **Настройка окружения**
   Приложение использует Vite, который автоматически загружает переменные из `.env` файла.
   
   Создайте файл `.env` в корне:
   ```
   GEMINI_API_KEY=ваш_ключ_от_google_ai
   ```

3. **Запуск локально**
   ```bash
   npm run dev
   ```

## 📱 Сборка Android (APK)

Для сборки через GitHub Actions ключ нужно добавить в **Settings -> Secrets and variables -> Actions** репозитория под именем `GEMINI_API_KEY`. В коде он не должен храниться.
