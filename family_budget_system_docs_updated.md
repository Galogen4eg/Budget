### Системная документация для фронтенд-приложения "Семейный бюджет" (обновленная версия)

**1. Архитектура приложения**  
- **Технологический стек**: React (TypeScript), Firebase (аутентификация через Google, Firestore, Storage)  
- **UI-библиотеки**: MUI (Material-UI) для компонентов, FullCalendar для календарей, react-hot-toast для уведомлений  
- **Адаптивность**: Mobile-first подход с использованием CSS Grid/Flexbox. Двухколоночный реестр сохраняется на всех устройствах (горизонтальный скролл на мобильных).  
- **Безопасность**: Firebase Rules настроены для аутентифицированных пользователей. Все запросы к Firestore валидируются на стороне клиента.  

**2. Структура данных в Firebase Firestore**  
```typescript
// Коллекция: rooms/{roomId}
interface Room {
  id: string;
  settings: {
    initialBalance: number; // Начальный остаток (только в настройках)
    savingsRate: number;    // Процент для откладывания (0-100)
    roomName: string;       // Название комнаты
    telegram: {             // Настройки Telegram
      botToken: string;
      chatId: string;
    };
  };
  participants: Participant[]; // Участники планировщика
}

// Коллекция: transactions/{transactionId} (только пользовательские операции)
interface Transaction {
  id: string;
  roomId: string;
  date: Date;              // Timestamp Firestore
  type: 'income' | 'expense';
  amount: number;
  category: string;        // Категория из предустановленного списка
  description: string;
  balanceAfter: number;    // Баланс после операции (рассчитывается при сохранении)
}

// Коллекция: recurringTransactions/{recurringId} (шаблоны обязательных трат)
interface RecurringTransaction {
  id: string;
  roomId: string;
  name: string;            // Название траты
  amount: number;
  category: string;
  dayOfMonth: number;      // День месяца для списания (1-31)
}

// Коллекция: shoppingItems/{itemId} (текущий список покупок)
interface ShoppingItem {
  id: string;
  roomId: string;
  name: string;
  quantity: number;
  unit: 'pcs' | 'liters' | 'grams' | 'kg'; // Единицы измерения
  isBought: boolean;
}

// Коллекция: shoppingHistory/{historyId} (история покупок для анализа)
interface ShoppingHistory {
  roomId: string;
  itemName: string;
  purchaseDate: Date;
}

// Коллекция: plannerEvents/{eventId} (события планировщика)
interface PlannerEvent {
  id: string;
  roomId: string;
  title: string;
  start: Date;             // Начало события
  end: Date;               // Окончание события
  participantIds: string[]; // ID участников
  isTemplate: boolean;     // Является ли шаблоном
  templateName?: string;   // Название шаблона (если есть)
}

// Коллекция: participants/{participantId} (участники)
interface Participant {
  id: string;
  roomId: string;
  name: string;
  color: string;           // HEX-код цвета (#FF5733)
}
```

**3. Страницы и компоненты**  
**3.1. Аутентификация**  
- **Вход через Google**: При первом заходе пользователь проходит аутентификацию через Google. `roomId` может быть связан с пользовательским аккаунтом или генерироваться и сохраняться в `localStorage`.  
- **Сохранение состояния**: При обновлении страницы приложение проверяет наличие аутентифицированного сеанса Firebase для восстановления сессии.  
- **UI**: Экран входа содержит кнопку "Войти через Google". После успешной аутентификации редирект в приложение происходит автоматически.  

**3.2. Шапка приложения**  
- **Элементы**: Три вкладки (Бюджет, Список покупок, Планировщик), иконка настроек (справа), аватар пользователя (справа).  
- **Запрещено**: Название комнаты и кнопка выхода в шапке. Они перемещены в раздел настроек.  
- **Стили**: Современный дизайн с тенями, скругленными углами, плавными переходами.  

**3.3. Вкладка "Бюджет"**  
```tsx
<Grid container spacing={3}>
  {/* Плитки для агрегированных данных */}
  <Grid item xs={12} md={3}>
    <BalanceTile 
      title="Начальный остаток" 
      value={initialBalance} 
      editable={false} // Только в настройках
    />
  </Grid>
  <Grid item xs={12} md={3}>
    <BalanceTile 
      title="Текущий баланс" 
      value={currentBalance} 
      editable={false}
    />
  </Grid>
  <Grid item xs={12} md={3}>
    <BalanceTile 
      title="Накопления" 
      value={savingsAmount} // = currentBalance * (savingsRate / 100)
      editable={false}
    />
  </Grid>
  <Grid item xs={12} md={3}>
    <BalanceTile 
      title="Ежедневный лимит" 
      value={dailyLimit} // = (currentBalance - sum(recurringExpenses)) / daysLeftInMonth
      editable={false}
    />
  </Grid>

  {/* Календарь бюджета (только месяц) */}
  <Grid item xs={12}>
    <BudgetCalendar 
      month={selectedMonth} 
      transactions={transactions} 
      recurringTransactions={recurringTransactions}
    />
  </Grid>

  {/* Категории */}
  <Grid item xs={12} md={6}>
    <CategoriesSummary 
      categories={[
        { name: 'Продукты', amount: 15200 },
        { name: 'Транспорт', amount: 3500 },
        // ... другие категории
      ]}
    />
  </Grid>

  {/* История операций */}
  <Grid item xs={12} md={6}>
    <TransactionHistory 
      transactions={sortedTransactions} // Сортировка: новые сверху
      onRowClick={openEditModal} 
      defaultPeriod="currentMonth" // Операции за месяц, выбранный в календаре
    />
    <SearchBar onSearch={filterTransactions} placeholder="Поиск операций..." />
  </Grid>

  {/* Обязательные траты (в настройках бюджета) */}
  <SettingsSection title="Обязательные траты">
    <RecurringTransactionsList 
      items={recurringTransactions}
      onAdd={openRecurringModal}
      onEdit={openRecurringModal}
      onDelete={confirmDeleteRecurring}
    />
  </SettingsSection>
</Grid>
```
- **Календарь бюджета**:  
  - Только режим "месяц". Кнопка "Сегодня" удалена.  
  - Дни с операциями помечаются точками. Нет интерактива (только просмотр).  
- **Категории**:  
  - Формат: `Название категории (слева) — Сумма (справа)`.  
  - Агрегация по всем операциям (включая обязательные траты за период).  
- **Обязательные траты**:  
  - Хранятся как шаблоны в `recurringTransactions`.  
  - При отображении истории операций генерируются виртуальные операции на лету.  
  - Нет дефолтных значений. Пустой список при первом запуске.  
- **Ежедневный лимит**:  
  ```ts
  dailyLimit = (currentBalance - sum(recurringExpensesForRemainingDays)) / daysLeftInMonth
  ```

**3.4. Вкладка "Список покупок"**  
```tsx
<Grid container spacing={2}>
  {/* Левая колонка: Добавление товара + Словарь */}
  <Grid item xs={12} sm={6} md={4}>
    <QuickAddForm 
      units={['pcs', 'liters', 'grams', 'kg']} 
      onAdd={addItem}
    />
    <DictionarySearch 
      items={defaultDictionary.concat(frequentItems)} 
      onSelect={fillQuickAddForm}
    />
  </Grid>

  {/* Правая колонка: Список покупок */}
  <Grid item xs={12} sm={6} md={8}>
    <ShoppingList 
      items={shoppingItems} 
      onToggleBought={markAsBought}
      onRowClick={openEditItemModal} // Редактирование по клику на строку
    />
    <SendToTelegramButton 
      isEnabled={!!telegramSettings.botToken && !!telegramSettings.chatId}
      onClick={sendListToTelegram}
    />
  </Grid>
</Grid>
```
- **Быстрое добавление**:  
  - Поля: Название (с автодополнением из словаря), Количество (подсказка: "1"), Единица измерения (выпадающий список).  
  - Склонение единиц:  
    ```ts
    const getUnitLabel = (unit: string, count: number) => {
      if (unit === 'pcs') {
        if (count % 10 === 1 && count % 100 !== 11) return 'штука';
        if ([2, 3, 4].includes(count % 10) && ![12, 13, 14].includes(count % 100)) return 'штуки';
        return 'штук';
      }
      return unit; // Для других единиц без склонения
    };
    ```
- **Словарь**:  
  - Статический список (200+ товаров) + топ-10 частых покупок из `shoppingHistory`.  
  - Поиск по названию.  
- **Отправка в Telegram**:  
  - Запрос к Telegram API с фронтенда (предупреждение о безопасности в настройках).  
  - Формат сообщения:  
    ```
    🛒 Список покупок:
    - Молоко: 1 штука
    - Хлеб: 2 штуки
    ```

**3.5. Вкладка "Планировщик"**  
```tsx
<Box>
  <CalendarViewSwitcher 
    views={['month', 'week', 'day']} 
    defaultView="month"
    onDateChange={loadEventsForPeriod}
  />
  <FullCalendar
    events={events}
    onCellClick={openEventModal} // Дата события = дата клика
    eventContent={renderEvent} // Цвет события = цвет первого участника
    headerToolbar={{
      left: 'prev,next',
      center: 'title',
      right: 'dayGridMonth,timeGridWeek,timeGridDay'
    }}
    slotDuration="01:00:00" // Шаг 1 час в режиме недели
  />
</Box>
```
- **Календарь**:  
  - Режимы: Месяц/Неделя/День. Кнопка "Сегодня" удалена.  
  - При клике на ячейку:  
    - Открывается модальное окно создания события.  
    - `start` события = дата и время клика (локальное время без смещения UTC).  
    - В режиме недели: время кратно часу клика (09:00, 10:00 и т.д.).  
  - Цвет событий:  
    - Если 1 участник — цвет фона = цвет участника.  
    - Если >1 участника — цвет не применяется (отображается иконка пользователей).  
- **Модальное окно события**:  
  - Поля: Название, Дата/время (начало/конец), Участники (мультивыбор), Чекбокс "Сделать шаблоном" + поле названия шаблона.  
  - Шаблоны: При выборе заполняются название и участники из шаблона.  
- **Участники**:  
  - Цвет выбирается через палитру (HEX-коды), а не список названий.  
  - Управление в настройках планировщика.  

**3.6. Настройки (единый раздел)**  
```tsx
<SettingsTabs>
  <Tab label="Бюджет">
    <InitialBalanceInput value={initialBalance} onChange={updateBalance} />
    <SavingsRateInput value={savingsRate} onChange={updateRate} placeholder="10" />
    <RecurringTransactionsSection /> 
    <DangerButton 
      label="Очистить операции" 
      onConfirm={clearAllTransactions} // Удаляет все документы из transactions
      confirmationText="Удалить все операции за всё время?"
    />
  </Tab>
  <Tab label="Покупки">
    <TelegramSettingsForm 
      botToken={telegram.botToken} 
      chatId={telegram.chatId}
      onSave={updateTelegramSettings}
    />
  </Tab>
  <Tab label="Планировщик">
    <ParticipantsManager 
      participants={participants}
      onAdd={openParticipantModal}
      onEdit={openParticipantModal}
      onDelete={confirmDeleteParticipant}
    />
    <TemplatesManager 
      templates={templates} 
      onRowClick={openTemplateModal} // Редактирование по клику на строку
    />
  </Tab>
</SettingsTabs>
```
- **Общие правила**:  
  - Все кнопки "Добавить" без символа "+".  
  - Редактирование по клику на любую ячейку строки (кроме иконок действий).  
  - Удаление всегда требует подтверждения.  
- **Состояние**:  
  - При переключении вкладок календарь планировщика не влияет на календарь бюджета.  
  - История операций сортируется по убыванию даты.  

**4. Ключевые алгоритмы**  
**4.1. Загрузка выписки (Excel)**  
```ts
const parseExcel = (file: File) => {
  const reader = new FileReader();
  reader.onload = (e) => {
    const data = new Uint8Array(e.target.result);
    const workbook = XLSX.read(data, { type: 'array' });
    const sheet = workbook.Sheets[workbook.SheetNames[0]];
    const rows = XLSX.utils.sheet_to_json(sheet, { header: 1 });
    
    // Строки с 20-й (индекс 19)
    const transactions = rows.slice(19).map(row => {
      const date = parseDate(row[0]); // Столбец A
      const category = row[3];        // Столбец D
      const description = row[4];     // Столбец E
      const amount = parseFloat(row[5].replace(',', '.')); // Столбец F
      
      // Определение типа операции
      const type = /зарплата|поступление/i.test(description) ? 'income' : 'expense';
      
      return { date, category, description, amount, type };
    });
    
    showPreviewModal(transactions); // Предварительный просмотр перед импортом
  };
  reader.readAsArrayBuffer(file);
};
```

**4.2. Расчет балансов**  
```ts
const calculateBalanceAfter = (transactions: Transaction[], initialBalance: number) => {
  let balance = initialBalance;
  return transactions.sort((a, b) => a.date - b.date).map(tx => {
    balance += tx.type === 'income' ? tx.amount : -tx.amount;
    return { ...tx, balanceAfter: balance };
  });
};
```

**4.3. Исправление дат в календаре планировщика**  
```ts
// В обработчике клика по ячейке FullCalendar
const handleCellClick = (info) => {
  // Конвертация в локальное время без смещения UTC
  const localDate = new Date(info.date.valueOf() + info.date.getTimezoneOffset() * 60000);
  openEventModal({ start: localDate, end: new Date(localDate.getTime() + 60 * 60000) });
};
```

**5. Требования к UI/UX**  
- **Дизайн**:  
  - Цветовая схема: основной — #4361ee (синий), акценты — #06d6a0 (бирюзовый), ошибки — #ef476f.  
  - Шрифты: Inter для текста, Montserrat для заголовков.  
- **Адаптивность**:  
  - Мобильные устройства: двухколоночные блоки скроллятся горизонтально.  
  - Отступы: 16px на мобильных, 24px на десктопах.  
- **Интерактив**:  
  - Ховер-эффекты для кликабельных элементов.  
  - Анимация появления модальных окон (плавное затухание).  
  - Валидация форм с подсветкой ошибок.  
- **Доступность**:  
  - Контрастность цветов ≥ 4.5:1.  
  - ARIA-метки для иконок.  

**6. Обработка ошибок**  
- **Глобальный обработчик**:  
  ```ts
  window.onerror = (message, source, lineno, colno, error) => {
    toast.error(`Ошибка: ${error.message}`);
    logErrorToFirestore({ message, stack: error.stack }); // Отправка в Firebase
  };
  ```
- **Сценарии**:  
  - Сбой Firebase: показать оффлайн-режим.  
  - Невалидные данные: откатить изменения и показать toast с описанием.  
  - Проблемы с календарем: резервный рендеринг через библиотеку date-fns.  

**7. Безопасность**  
- **Firebase Rules** (пример для Firestore):  
  ```firestore
  match /rooms/{roomId} {
    allow read, write: if request.auth != null && 
                      request.auth.uid != null;
  }
  ```
- **Данные в localStorage**:  
  - Хранить только `roomId` и аутентификационные данные пользователя. Секреты (токены Telegram) — только в Firestore с правилами доступа.  
- **Отправка в Telegram**:  
  - Предупреждать пользователя: «Токен бота будет храниться в облаке. Не используйте продакшен-бота!»  

**8. Оптимизация**  
- **Ленивая загрузка**:  
  - Календари и тяжелые компоненты подгружаются через `React.lazy`.  
- **Кэширование**:  
  - Частые запросы (категории, участники) кэшируются в Zustand.  
- **Оптимизация рендеринга**:  
  - `React.memo` для таблиц и списков.  
  - Виртуализация для истории операций (react-virtualized).  

**9. Тестирование**  
- **Unit-тесты**:  
  - Расчет балансов, склонение единиц измерения, парсинг Excel.  
- **Интеграционные тесты**:  
  - Добавление операции → обновление баланса.  
  - Создание события в календаре → корректная дата в модалке.  
- **Ручное тестирование**:  
  - Сценарий: "Обновление страницы не сбрасывает сессию".  
  - Сценарий: "Удаление обязательной траты требует подтверждения".  

**10. Развертывание**  
- **Хостинг**: Firebase Hosting с кэшированием статики.  
- **Сборка**:  
  ```bash
  npm run build -- --output-path=dist
  firebase deploy --only hosting
  ```
- **Мониторинг**:  
  - Firebase Performance Monitoring для отслеживания скорости.  
  - Sentry для логирования ошибок.  

---  
**Важно**:  
- Приложение **только фронтенд**. Бэкенд-логика реализована через Firebase SDK.  
- Все дефолтные данные (обязательные траты, участники) удалены. Инициализация — пустыми массивами/объектами.  
- Все доработки выполнены строго по ТЗ. Никакие дополнительные функции не добавлены.  
- При возникновении критической ошибки (например, сбой календаря) приложение показывает fallback-интерфейс с кнопкой перезагрузки.